import React from 'react'

const Winlose = _ => {
    return (
        <>
            <h1>win/lose here</h1>
            {/* action log:
            compare engine and tire,
            items used
            compare overall stats and declare winner
            show how much money play won
            */}

            {/* 
            if (yourengine > npcengine) {
                logarr.push('playername has better engine, playername pull ahead')
            } else {
                logarr.push('npcname start off strong, npcname pull ahead')
            }

            if (yourtire > npctire) {
                logarr.push('playername speed up thanks to superior tires')
            } else {
                logarr.push('npcname speed increased, watch out!')
            }

            if (state.itemused !== null) {
                switch(state.itemused) {
                    case 'oil spill':
                        logarr.push(`playername used ${state.itemused}, npcname got slowed!`)
                        break;
                    case 'nitro boost':
                        logarr.push(`playername used ${state.itemused} and increase speed by 5%`)
                        break;
                    case 'grippy tires':
                        logarr.push(`playername used ${state.itemused}, and increase speed by 2%`)
                        break;
                    default:
                        break;
                }
            }
            */}
            {/* result animation*/}
        </>
    )
}

export default Winlose